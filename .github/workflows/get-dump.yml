name: Braindump Builder

on:
  push:
    paths:
      - 'source.txt'
  
permissions:
  contents: write
  
jobs:
  call_reusable_workflow:
    uses: https://github.com/sarvjan/template/.github/workflows/get-dump.yml@main
    with:
      config: "source.txt"
